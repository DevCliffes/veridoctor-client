# VERIDOCTOR DESIGN PACKAGE.

## Adding a new component from shadcn ui

- @example: adding button component

```
pnpm dlx shadcn@latest add button
```

NOTE: ensure the import path for cn inside the component is relative and not aliased with the @ symbol.
